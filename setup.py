from setuptools import setup, find_packages

setup(
    name='translator',
    version='1.0',
    packages=find_packages(),
    author='Juan',
    description='A package for translating text between English and French',
    # Add any other relevant information about the package
)
